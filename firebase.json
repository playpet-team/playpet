{
  "functions": {
    "source": "packages/firefunction",
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint",
      "npm --prefix \"$RESOURCE_DIR\" run build"
    ],
    "ignore": [
      "src",
	    "node_modules"
    ]
  }
}
