require("ts-node").register({
  compilerOptions: {
    module: "commonjs",
    target: "es2017",
  },
})

// const { createPages } = require("./src/lib/createPages/jobDetail/createPages");

// exports.createPages = createPages;
