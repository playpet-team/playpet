import React from "react"
import styled from "@emotion/styled"

const HomeBlock = styled.div`
  flex: 1;
  height: 100vh;
  justify-content: center;
  align-items: center;
  font-size: 60px;
  font-weight: 700;
`

export default function Home() {
  return <HomeBlock>멍멍멍!</HomeBlock>
}
